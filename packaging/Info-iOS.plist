<?xml version="1.0" encoding="UTF-8"?>
<!-- iOS-specific Info.plist additions to merge with Makepad's generated plist -->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required Bundle OS Type code -->
    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <!-- Asset catalog app icon name (required for iOS 11+) -->
    <key>CFBundleIconName</key>
    <string>AppIcon</string>

    <!-- Launch screen configuration (iOS 14+) -->
    <key>UILaunchScreen</key>
    <dict>
        <key>UIImageName</key>
        <string>AppIcon60x60</string>
        <key>UIColorName</key>
        <string>LaunchScreenBackground</string>
    </dict>

    <!-- Location access for sharing current location in Matrix rooms -->
    <key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
    <string>Robrix needs permission to share your current location to a Matrix room.</string>
    <key>NSLocationWhenInUseUsageDescription</key>
    <string>Robrix needs permission to share your current location to a Matrix room.</string>
    <key>NSLocationUsageDescription</key>
    <string>Robrix needs permission to share your current location to a Matrix room.</string>
    <key>NSLocationDefaultAccuracyReduced</key>
    <false/>

    <!-- App uses only standard encryption (HTTPS/TLS) -->
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
</dict>
</plist>
